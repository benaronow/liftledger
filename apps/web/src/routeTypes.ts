export enum RouteType {
  Welcome = "/welcome",
  Progress = "/progress",
  History = "/history",
  Dashboard = "/dashboard",
  Add = "/edit-program",
  Profile = "/profile",
  Settings = "/settings",
  Workout = "/complete-day",
}
