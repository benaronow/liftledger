import { COLORS } from "@liftledger/shared";

// One color per gym line, cycled if there are more gyms than colors (web parity).
export const GYM_COLORS = [
  COLORS.primary,
  COLORS.secondary,
  COLORS.success,
  COLORS.warning,
];
